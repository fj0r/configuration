{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "zshdb",
            "request": "launch",
            "name": "Zsh-Debug (type in script name)",
            "cwd": "${workspaceFolder}",
            "program": "${file}",
            "args": []
        }
    ]
}