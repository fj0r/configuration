{
    "tasks": [
        {
            "name": "ls",
            "cmd": "ls",
            "cwd": "${config_dir}/",
            "tags": ["dev"]
        },
        {
            "name": "test",
            "cmd": "pwd",
            "cwd": "${config_dir}/",
            "tags": ["dev"]
        }
    ]
}
